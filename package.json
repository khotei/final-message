{
  "name": "hirely",
  "version": "0.0.1",
  "engines": {
    "node": ">=22"
  },
  "packageManager": ">=npm@10.0.0",
  "workspaces": [
    "app/**",
    "packages/**"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
