{
  "name": "final-message",
  "version": "0.0.1",
  "engines": {
    "node": ">=22"
  },
  "packageManager": ">=npm@10.0.0",
  "workspaces": [
    "app/**",
    "packages/**"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
